---
name: Protocol change
about: Any change to metrics, thresholds, seeds or splits after M2
labels: protocol, needs-adr
---

⚠ The protocol is frozen at M2. A change here invalidates every completed arm.

**What changes**

**Why it cannot wait until after the current runs**

**ADR** (link to `docs/decisions/ADR-xxxx`)

**Re-runs required** (list every detector arm that must be re-run)
